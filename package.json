{
  "name": "k6-typescript-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-start": "mkdir -p src/output && npx tsc && k6 --out json=src/output/result.json --out csv=src/output/result.csv run dist/Index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/k6": "^0.49.2",
    "@types/node": "^20.11.28",
    "typescript": "^5.4.2"
  }
}
